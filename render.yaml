services:
  - type: web
    name: hasil-tka-mumtaza
    env: python
    buildCommand: |
      apt-get update && apt-get install -y libreoffice-writer && pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12
